# Default: normalize line endings to LF in the repo
* text=auto

# Shell scripts must always use LF — CRLF breaks bash on macOS/Linux
*.sh text eol=lf

# YAML files used by EAS build configs
*.yml text eol=lf
*.yaml text eol=lf
